# Portal — HSCT(및 향후 다른 프로젝트) 통합 UI 셸

## 0. 이 문서의 목적

여러 프로젝트(HSCT 등)를 하나의 화면 안에서 오갈 수 있는 새 프론트엔드 셸(shell)을
만드는 작업이다. 2026-09-22 워드픽 세션(1차)에서 사용자와 레이아웃·범위를 확인만 하고
**실제 구현은 진행하지 않았다** — "다른 세션에서 시작할 거라 일단 네가 얼마나 이해하는지
물어본 것"(사용자 원문)이라, 다음 세션이 이 문서 하나만 읽고 바로 구현에 들어갈 수 있게
확인된 내용을 정리해둔다. 같은 날 2차 세션에서 §3의 미확정 사항 대부분을 확정했다(§4
작업 로그 참고) — **단, 이 2차 세션에서도 구현(코드 작성)은 시작하지 않았다.**

## 1. 레이아웃 (사용자가 그림으로 직접 제시, 화면을 4등분)

```
┌────────┬──────────────────────────────────────┐
│   ①    │              ②                        │
│  로고   │         대분류(앱 선택)                │
├────────┼──────────────────────────────────────┤
│        │                                        │
│   ③    │              ④                        │
│ 중분류  │         선택한 기능이 실제로            │
│ (기능   │         동작하는 메인 화면              │
│  목록)  │                                        │
│        │                                        │
└────────┴──────────────────────────────────────┘
```

- **① 로고(좌상단)**: 클릭하면 홈으로 이동하는 버튼.
- **② 상단바(로고 오른쪽, 가로 전체)**: **대분류** — 여러 프로젝트("앱")를 나열해 어느
  것을 쓸지 고르는 최상위 메뉴. 사용자 확인: "여러 프로젝트 통합"이 맞다(HSCT 하나만이
  아니라 향후 다른 실제 배포된 앱들도 여기 추가될 예정) — 단, **어떤 프로젝트들을 정확히
  포함할지는 아직 정하지 않았고, 한꺼번에 다 정하지 않고 "필요한 내용을 하나씩" 만들어가기로
  확정**했다(사용자 원문). 지금 확정된 건 **첫 번째로 HSCT부터 시작**한다는 것뿐이다.
- **③ 좌측 사이드바(②에서 고른 앱에 종속, 세로 전체)**: **중분류** — 그 앱 안의 개별
  기능 목록. HSCT를 예로 들면 지금 `public/index.html`에 있는 섹션들이 후보:
  1. 발주 데이터 엑셀 업로드(order-export, 17컬럼)
  2. 레거시 업로드(URL·상품명·도착국 3컬럼)
  3. 일본어 상품명 → 한국어 번역
  4. 학습 데이터 직접 업로드
  5. 주문내역 재검토(AI분석실패) — order-status, 2026-09-22 신설분(가장 최근 기능)
- **④ 메인 영역(우측 대부분)**: ③에서 고른 기능이 실제로 동작하는 화면. **페이지 전체
  리로드 없이 같은 자리에서 상태가 바뀌는 방식**을 사용자가 직접 예시로 듦: "발주데이터
  업로드하고, 업로드 완료되면, 페이지가 바뀌면서 제안 결과 확인 및 편집으로 넘어간다."

## 2. 확정된 원칙

1. **기존 앱들의 주소는 그대로 둔다.** 지금의 `E:\Tools\HScodeTOOL\public\index.html`
   (및 그 API들)은 손대지 않는다 — URL도 코드도 그대로.
2. **이 포털은 HSCT와 완전히 별도의 프로젝트/저장소로 구성한다**(2026-09-22 2차 세션
   확정 — 최초 제안이었던 "HSCT `public/` 폴더 안에 정적 파일만 추가" 방식에서 변경됨).
   - **파일 위치**: 이 `WORDPICK SERVICES` 폴더(저장소 `daeanjun/WORDPICK_SERVICES`)
     안에 새 프런트엔드로 구성한다. HSCT 저장소는 전혀 건드리지 않는다.
   - **백엔드 연동 방식**: 별도 출처(origin)이므로, HSCT API는 크로스오리진으로
     절대경로 호출한다 — 운영 주소 `https://hs-code-tool-production.up.railway.app`
     (`HScodeTOOL/README.md`에서 확인). HSCT의 `server.js`에 이미 `app.use(cors())`가
     걸려 있어(전체 출처 허용) 이 방식이 코드 수정 없이 바로 동작한다 — 이 사실은
     `HScodeTOOL/server.js`를 직접 읽어 확인했다(추정 아님).
   - **CSS**: 기존 `HScodeTOOL/public/styles.css`를 재사용하지 않고, 이 저장소 안에
     새 스타일시트를 신설한다(2026-09-22 2차 세션 확정).
3. **한꺼번에 다 만들지 않고 단계적으로 간다**(사용자 명시: "앱 목록은 한꺼번에 하기보다
   필요한 내용을 하나씩 만들어가자").
   - 1단계: ①②③④ 뼈대만 먼저(정적 레이아웃) — 상단바(②)엔 우선 **HSCT 하나만**,
     사이드바(③)엔 §1의 4개 기능 목록을 넣되, ④는 아직 빈 화면/자리표시자로.
   - 2단계: **"발주 데이터 엑셀 업로드"를 첫 실제 동작 기능으로** ④에 연결한다
     (2026-09-22 2차 세션 확정 — 1차 세션에서 제안했던 "주문내역 재검토"에서 변경됨).
     나머지 기능은 "준비 중" 표시만 해두고 이후 하나씩 연결.
4. **기술 스택/배포는 아직 확정되지 않았다** — §3 참고. 오늘(2026-09-22 2차 세션)은
   계획 확정까지만 진행하고, 실제 구현(코드 작성)은 다음 세션에서 시작하기로 사용자가
   명시했다.

## 3. 아직 미확정 — 다음 세션 시작 시 확인할 것

1. **포함할 프로젝트(앱) 전체 목록**: HSCT 다음으로 어떤 프로젝트를 추가할지(예:
   wordPICK-service 등 실제 배포된 다른 Railway 프로젝트가 후보로 언급됐으나 확정 아님).
2. **기술 스택/검증 순서(2026-09-22 2차 세션 중 사용자 지시로 변경 → 같은 세션 안에서
   재변경)**:
   "처음 하는 거니까 뼈대를 로컬에서 잡고 서버로 이어가자" → 1차로 순수 정적
   (HTML/JS/CSS, 서버 로직 없음)으로 로컬에서 뼈대를 만들었다(§5 작업 로그 참고).
   이후 확인용 URL을 요청받아 GitHub Pages를 시도하는 과정에서 **Claude가 처음
   안내했던 내용이 부정확했음이 실제 API 응답으로 드러났다**: "비공개 저장소도 Pages
   배포는 되고 페이지 URL만 공개된다"고 안내했으나, 실제로는 **GitHub Free 플랜에서는
   비공개 저장소에 Pages 자체가 전혀 활성화되지 않는다**(`POST .../pages` 호출이
   `"Your current plan does not support GitHub Pages for this repository."` 422로
   거부됨). 이 사실을 다시 안내하고 재확인받은 뒤, 사용자가 **"저장소를 Public으로
   전환하고 Pages 진행"으로 확정** — 그 결과 **이 저장소는 2026-09-22부로 공개
   (PUBLIC)로 전환됐다.** 소스 코드(HTML/CSS/JS, 커밋 이력 포함)가 전부 공개된
   상태이므로, 이후 민감한 내용(내부 API 키, 실제 고객 데이터 등)을 이 저장소에
   커밋하지 않도록 주의할 것.
   - GitHub Pages는 **Actions 기반 배포**로 설정했다(`.github/workflows/deploy-pages.yml`)
     — 정적 파일이 저장소 루트가 아니라 `public/` 폴더에 있어서, 브랜치 기반 Pages
     (루트 또는 `/docs`만 지원)로는 불가능했기 때문. `master` 브랜치의 `public/**` 변경
     시 자동 배포된다.
   - 배포 URL: `https://daeanjun.github.io/WORDPICK_SERVICES/`
   - Node/Express 서버 전환은 그다음 단계로 미룬다(정적 → Express 전환은 파일 구조를
     그대로 두고 얇은 서버만 씌우면 되므로 복잡도가 낮다고 Claude가 설명했고, 사용자가
     이 순서에 동의함).
3. **배포 대상(Railway 등)은 여전히 미결정** — GitHub Pages는 어디까지나 확인용이며,
   실제 운영 배포처는 별도로 다시 논의한다.
4. 실행 게이트 원칙: 1단계(로컬 정적 뼈대) 구현은 2차 세션 중 사용자가 "뼈대를 로컬에서
   잡고 서버로 이어가자"고 명시적으로 지시해 **시작 승인을 받았다**. 저장소 공개 전환과
   GitHub Pages 활성화도 같은 세션에서 사용자가 명시적으로 확정했다. 단 이 승인들은
   여기까지이며, Node/Express 서버 도입이나 Railway 등 실제 배포는 별도로 다시
   확인받을 것.

## 4. 폴더 구조 (1단계 구현 후 기준)

```
WORDPICK SERVICES/
├── README.md          # 이 문서
├── .github/workflows/deploy-pages.yml  # GitHub Pages 자동 배포(§2.2)
└── public/
    ├── index.html      # 4분할 셸의 정적 골격(①로고+햄버거 토글 ②앱탭 ③사이드바 ④메인)
    ├── styles.css      # 새로 만든 스타일시트(HSCT의 styles.css와 무관, CSS Grid 4분할,
                         # 다크 테마 + 사이드바 접힘/아이콘 모드 포함)
    └── portal.js       # APPS 데이터(HSCT 탭 1개, 항목 4개: group 1(하위 3개)+feature 2+
                         # embed 1)를 기반으로 탭·사이드바(펼침/아이콘 전용 2가지 모드)·
                         # 메인 영역을 렌더링/전환하는 순수 JS. 서버 호출은 아직 전혀
                         # 없다(1단계 범위 밖) — 단 "관리자 페이지"만 예외로, HSCT 운영
                         # 서버의 admin.html을 ④ 영역 안에 iframe으로 띄우는 실제 동작.
```

- 서버 로직은 아직 없다(1단계는 순수 정적 파일뿐). 실행하려면 아무 정적 서버로 `public/`
  폴더를 서빙하면 된다 — 예: `npx serve public`.
- 사이드바 항목 구조(8차 이후 확정, §5의 8/11차 작업 로그 참고):
  - **WGEX 주문내역 업로드**(그룹, 펼침/접힘) → DHOLIC 주문내역 업로드(`order-status`) /
    LEGACY FORM(`legacy-upload`) / QUERY DATA(`order-export`)
  - **상품명 번역기**(`translate`), **학습 데이터 수동 업로드**(`train`) — 둘 다
    `status: 'pending'`, 클릭하면 ④ 영역에 "아직 연결되지 않았습니다" 안내만 표시.
    `status`가 `pending`인 항목은 사이드바에 별도 배지를 표시하지 않는다(11차에서
    "준비 중" 문구 제거).
  - **관리자 페이지**(`admin`, `type: 'embed'`, `status: 'ready'`) — 클릭하면 페이지
    이동 없이 ④ 영역 안에 `https://hs-code-tool-production.up.railway.app/admin.html`이
    iframe으로 그대로 뜬다. 유일하게 "연결됨" 배지가 붙는 항목.
- 사이드바는 상단 햄버거 버튼으로 펼침(텍스트+배지) ↔ 아이콘 전용(폭 68px) 모드를
  전환할 수 있다. 아이콘 전용 모드에서는 그룹(WGEX)·feature 2개·embed 1개, 총 4개만
  아이콘으로 보이고, 그룹 아이콘을 클릭하면 자동으로 펼침 모드로 돌아가 하위 항목을
  보여준다(embed/feature 아이콘은 클릭해도 접힘 상태를 유지한다).
- 실제 API 연결은 2단계(§2.3) 이후 작업(관리자 페이지 embed 제외).

## 5. 작업 로그

### 2026-09-22 (1차) — 레이아웃·범위 확인만 진행(구현 없음), 이 폴더·문서 신설

사용자가 4분할 레이아웃 그림을 제시하고 이해도만 확인 요청 → 설명 후 두 차례
AskUserQuestion으로 범위 확인(여러 프로젝트 통합 확정, 단 목록은 미정 / HSCT부터 시작
확정) → 사용자가 "일단 구현은 하지마"로 명시 중단 → "다른 세션에서 시작할 거라 확인만
한 것"이라며 이 요약 문서 작성 요청. 코드는 전혀 작성하지 않았다(기존 `styles.css`
참고차 읽기만 함).

### 2026-09-22 (2차) — §3 미확정 사항 확정 + 1단계(로컬 정적 뼈대) 구현

1. HSCT의 `public/index.html`·`styles.css`·`server.js`를 직접 읽고 실제 기능 목록(7개
   섹션 → 중복/중첩 제외하면 4개 상위 기능)과 `app.use(cors())`가 이미 걸려 있는 사실을
   확인.
2. AskUserQuestion으로 미확정 사항을 순차 확인: 첫 연결 기능(주문내역 재검토 → **발주
   데이터 엑셀 업로드**로 변경), 파일 경로(HSCT `public/` 안 → **이 저장소 자체**로
   변경), CSS(재사용 → **새 스타일시트 신설**), 구현 시작 여부(처음엔 "계획만, 구현은
   나중에"로 답했다가, 바로 다음 메시지에서 "처음 하는 거니까 뼈대를 로컬에서 잡고
   서버로 이어가자"로 번복 — 이후 답변을 우선해 진행).
3. 사용자가 "GitHub에 push해서 GitHub Pages로 동작 확인이 가능한지" 질문 → `gh repo
   view`로 이 저장소가 PRIVATE임을 확인하고, Free 플랜 기준 비공개 저장소도 Pages
   배포 시 렌더링된 페이지 URL은 공개된다는 사실을 안내 → 사용자가 최종적으로 "로컬에서
   먼저 확인"(GitHub Pages 확장은 나중 단계)으로 확정.
4. `public/index.html`·`public/styles.css`·`public/portal.js` 신규 작성(1단계 범위:
   정적 뼈대만, 실제 API 연결 없음).
5. 검증: ① `node --check`로 `portal.js` 문법 확인, ② `npx serve public`으로 로컬
   정적 서버를 띄우고 `curl`로 3개 파일 모두 HTTP 200 확인, ③ jsdom으로 `portal.js`를
   실제 DOM에서 실행시켜 탭/사이드바 렌더링과 클릭 시 ④ 영역 전환(기능명·"준비 중"
   배지·`is-active` 클래스·홈 리셋)을 assert 기반으로 검증 — 전부 통과. **단, 실제
   브라우저로 시각적 렌더링(그리드 배치·색상 등)은 이 환경에 브라우저/스크린샷 도구가
   없어 확인하지 못했다** — 사용자가 `npx serve public` 후 직접 열어보는 것을 권장한다.

### 2026-09-22 (3차) — 확인용 URL 요청 → GitHub Pages 배포(저장소 Public 전환 포함)

사용자가 "확인 가능한 URL은?"이라고 질문 → 처음엔 "로컬 실행 vs 지금 GitHub Pages 배포"로
선택지를 물었고 사용자가 "지금 GitHub Pages로 공개 URL 만들기"를 선택. `gh api POST
.../pages` 시도 결과 **"Your current plan does not support GitHub Pages for this
repository."(422)** 오류 발생 — 2차 세션에서 Claude가 안내했던 "비공개 저장소도 Pages는
되고 URL만 공개된다"는 설명이 **틀렸음이 실제 API 응답으로 드러남**(Free 플랜은 비공개
저장소에 Pages 자체가 비활성화됨). 이 사실을 정정해 다시 안내하고 재확인받은 뒤, 사용자가
"저장소를 Public으로 전환하고 Pages 진행"으로 확정 →
1. `gh repo edit --visibility public --accept-visibility-change-consequences`로 저장소
   공개 전환(확인됨: `gh repo view --json visibility` → `PUBLIC`).
2. `public/` 폴더가 저장소 루트가 아니라 브랜치 기반 Pages(루트/`docs`만 지원) 불가 →
   `.github/workflows/deploy-pages.yml` 신설(Actions 기반 배포, `master`의 `public/**`
   변경 시 자동 배포).
3. `gh api POST .../pages -f build_type=workflow`로 Pages 활성화 확인.
4. 커밋·푸시 후 `gh run watch`로 워크플로가 `success`로 완료됨을 확인, `curl`로
   `https://daeanjun.github.io/WORDPICK_SERVICES/`·`styles.css`·`portal.js` 3개 모두
   HTTP 200 실제 응답 확인.

**주의**: 이제 이 저장소는 소스 코드·커밋 이력이 전부 공개 상태다. 앞으로 API 키·실제
고객 데이터 등 민감한 내용을 커밋하지 않도록 유의할 것.

### 2026-09-22 (4차) — 포털 배색·톤 리디자인("화려하지 않지만 세련되고 집중력 있는 느낌")

사용자 요청에 따라 `public/styles.css`를 채도 높은 파란색(#2f6feb) 위주 배색에서
절제된 뉴트럴 톤(따뜻한 오프화이트 배경 `#f6f5f2` + 딥 슬레이트 네이비 포인트 컬러
`#2e3646`)으로 전면 재작성. 주요 변경: 포인트 컬러를 배경 채움 대신 하단/좌측 얇은
인디케이터 선과 텍스트 색으로만 절제해 사용, 알약형(pill) 배지를 소문자 대문자 변환 +
자간을 준 무채색 라벨로 교체, `box-shadow` 전면 제거하고 1px 헤어라인 보더로만 구획,
로고를 대문자 소문자간격(letter-spacing) 스타일로 변경. 레이아웃 구조(HTML/JS)는
변경하지 않았다.

검증: ① 중괄호 짝 맞춤(22:22) 스크립트로 CSS 구문 오류 없음 확인, ② `npx serve public`
로컬 서버 + `curl`로 `styles.css` HTTP 200 확인. **단, 실제 브라우저에서 "세련되고
집중력 있는 느낌"이라는 주관적 목표가 실제로 달성됐는지는 Claude가 시각적으로 확인할
수 없었다** — 이 환경에 브라우저/스크린샷 도구가 없기 때문. 사용자가 배포된
`https://daeanjun.github.io/WORDPICK_SERVICES/`(push 후 Actions가 자동 재배포)를 직접
열어보고 톤이 의도에 맞는지 확인 후 피드백을 주는 것이 필요하다.

### 2026-09-22 (5차) — 다크 톤 + 선택 시 색상 반전으로 재조정

사용자 요청: "바탕을 검정색은 아니지만 검정색에 가까운 짙은색으로", "글자 선택하면 색상
반전되게". `public/styles.css`의 색상 변수를 전면 다크 테마로 교체:
- 배경 `#f6f5f2`(오프화이트) → `#17181b`(순검정이 아닌 짙은 차콜), 패널 배경
  `#ffffff` → `#1d1f22`, 텍스트 `#232420`(짙은색) → `#ececea`(순백이 아닌 오프화이트).
- **선택(활성) 상태를 전부 "색상 반전"으로 통일**: 기존에는 얇은 보더/인디케이터 선만
  쓰던 활성 표시(`.app-tab.is-active`, `.sidebar-item.is-active`)를, 배경을
  `var(--color-invert-bg)`(밝은 오프화이트)로 채우고 글자색을 `var(--color-invert-text)`
  (짙은 배경색)로 뒤집는 방식으로 변경 — 실제로 "글자를 선택하면 색이 반전"되는
  효과를 준다.
- **글자체(폰트 패밀리·크기·굵기·자간)는 사용자가 "건들지 말 것"이라고 명시해 전혀
  변경하지 않았다** — 4차 배포분과 `font-family`/`font-size`/`letter-spacing`/
  `text-transform` 값이 동일함을 diff로 대조 확인.

검증: ① 중괄호 짝 맞춤(22:22)으로 CSS 구문 오류 없음 확인, ② `npx serve public` 로컬
서버 + `curl`로 `styles.css` HTTP 200 확인, ③ 4차 버전과 diff하여 타이포그래피 관련
속성(font-*, letter-spacing, text-transform)이 값 그대로 보존됐음을 직접 대조 확인.
**단, 실제 브라우저에서 다크 배경·반전 하이라이트가 시각적으로 의도대로 보이는지는
이전과 마찬가지로 Claude가 직접 확인하지 못했다** — 배포 후 사용자 확인 필요.

### 2026-09-22 (6차) — 스크린샷 피드백 반영: hover/반전 구분 명확화, HSCT 탭 박스 제거

사용자가 실제 배포 페이지 스크린샷을 공유하며 지적: "이건 오버행 정도인데, 반전이란
단어를 모르나?", "HSCT가 박스 안에 있는 건 너무 촌스러운데?".

원인 분석(스크린샷 재확인): 사용자가 캡처한 화면은 사이드바 항목에 **마우스만 올린
hover 상태**였다(④ 영역에 여전히 기본 안내 문구가 떠 있어, 실제 클릭·선택은 되지
않았음을 확인) — 5차에서 만든 진짜 반전(배경↔글자색 전환)은 클릭(선택) 시에만
적용되도록 설계돼 있었는데, 이게 hover와 혼동되기 쉬운 구조였다. 또한 HSCT 탭이 항상
박스로 보인 이유는, 앱이 HSCT 하나뿐이라 `portal.js`의 기본값(`activeApp: 'hsct'`)
때문에 상단 탭이 항상 "선택됨" 반전 박스 상태로 렌더링됐기 때문 — 탭에도 사이드바와
동일한 배경 반전 스타일을 썼던 것이 원인.

조치: **상단 앱 탭(`.app-tab`)의 활성 표시를 배경 반전 박스 방식에서 제거하고, 얇은
밑줄(2px, `border-bottom`) + 굵은 글자로 바꿨다** — 탭이 1개뿐이어도 박스로 갇혀
보이지 않는다. **사이드바 항목(`.sidebar-item`)의 진짜 반전(클릭 시 배경/글자색 전체
전환)은 그대로 유지** — 이건 의도대로 동작하고 있었고, 사용자가 본 화면은 hover였을
뿐이다. 글자체는 이번에도 변경하지 않았다.

검증: ① 중괄호 짝 맞춤(22:22)으로 CSS 구문 오류 없음 확인, ② `npx serve public` 로컬
서버 + `curl`로 `styles.css` HTTP 200 확인, ③ diff로 font-*/letter-spacing/
text-transform 속성이 변경되지 않았음을 대조 확인. **여전히 실제 브라우저로 hover와
클릭(선택) 상태가 시각적으로 명확히 구분되는지는 Claude가 직접 확인하지 못했다** —
사용자가 실제로 항목을 클릭해서 ④ 영역이 전환되는 것까지 함께 확인해 주는 것이
필요하다(클릭 전 hover만으로는 진짜 반전을 볼 수 없다).

### 2026-09-22 (7차) — 로고(WORDPICK) 클릭-홈 이동 재확인 + 글자 크기 확대

사용자가 "레이아웃 1의 워드픽을 로고로 쓰자. 글자가 이뻐서 해당 워드픽을 클릭하면
홈으로 가게 해줘"라고 요청 → 확인해보니 **로고 텍스트("WORDPICK")와 클릭 시 홈으로
가는 동작은 1단계 구현 때부터 이미 존재**했다(`index.html`의 `#homeLink`,
`portal.js`의 클릭 핸들러가 `activeFeature`를 `null`로 리셋). 다른 프로젝트
(`E:\Tools\WHSwordPICK`, `E:\Tools\wordPICK-service`)에 별도 로고 디자인이 있는지도
찾아봤으나 해당하는 자료는 없었다 — 즉 "레이아웃 1의 워드픽"은 지금 이 포털 좌상단에
이미 떠 있는 그 로고를 가리키는 것으로 판단했다.

추가로 요청받은 "워드픽 글자를 2단계만 키워보자"에 따라 `.logo`의 `font-size`를
`0.85rem` → `1.15rem`으로 확대. 클릭 가능함을 시각적으로 알 수 있도록 `.logo:hover`에
`opacity: 0.7` 전환 효과도 추가(글자체·자간·굵기 등은 변경하지 않음).

검증: ① 중괄호 짝 맞춤(23:23)으로 CSS 구문 오류 없음 확인, ② `npx serve public` 로컬
서버 + `curl`로 `styles.css` HTTP 200 확인, ③ jsdom으로 로고 클릭 시 ④ 영역이 초기
안내 문구로 리셋되는 동작을 다시 한 번 assert 기반으로 재검증(통과) — 이 로직은
1단계 이후 변경된 적이 없다. **실제 브라우저에서 로고 글자 크기가 자연스러운지는
Claude가 시각적으로 확인하지 못했다.**

### 2026-09-22 (8차) — 사이드바 계층 구조 재구성(WEAVING-X 참고) + 햄버거 아이콘 접힘 모드

사용자가 외부 업무 툴(WEAVING-X) 스크린샷과 "WORDPICK" 3단계 목업(①현재 4개 목록
→②라벨 교체+그룹화 →③펼친 하위메뉴)을 제시하며 "너무 따라하진 말고 내가 원하는
느낌에 이런 양식을 더해서" 요청. 화살표로 표시된 매핑을 그대로 반영:

- **WGEX 주문내역 업로드**(그룹, 기본 펼침, 헤더 클릭으로 접기/펼치기 가능)
  - DHOLIC 주문내역 업로드 = 기존 `order-status`(주문내역 재검토·AI분석실패)
  - LEGACY FORM = 기존 HSCT `index.html`의 "레거시 업로드 방식(URL·상품명·도착국
    3컬럼)" — 지금까지 포털 사이드바에 없던 항목이라 `legacy-upload`로 신규 추가
  - QUERY DATA = 기존 `order-export`(발주 데이터 엑셀 업로드)
- **상품명 번역기** = 기존 `translate`(일본어 상품명 → 한국어 번역) 라벨 교체
- **학습 데이터 수동 업로드** = 기존 `train`(학습 데이터 직접 업로드) 라벨 교체
- **관리자 페이지**(신규, `type: 'link'`) = `https://hs-code-tool-production.up.railway.app/admin.html`
  — 처음엔 새 탭으로 열도록 만들었으나, 사용자가 "새 탭이 아니라 해당 창에 불러오는
  방식으로"라고 정정해 **같은 창에서 이동하는 일반 링크**로 수정(`target="_blank"`
  제거). 이 항목은 `status` 배지가 없는 유일한 실제 동작 항목이다.

추가로 요청받은 "가로 줄 세 개 아이콘(햄버거)을 누르면 아이콘만 남는 방식" 구현:
로고 왼쪽에 햄버거 토글 버튼 신설, 클릭 시 사이드바가 240px(텍스트+배지) ↔
68px(아이콘 전용) 사이를 전환한다. 아이콘 전용 모드에서는 그룹 1개(WGEX)·feature
2개·link 1개, 총 4개만 아이콘으로 보이며(하위 3개는 숨김), 그룹 아이콘을 클릭하면
자동으로 펼침 모드로 돌아가며 그 그룹이 열려 있는 상태로 표시된다. 참고 스크린샷의
아이콘이 "너무 흐리다"는 지적에 따라, 아이콘 색상은 muted/faint 톤이 아니라 가장 밝은
텍스트 색(`var(--color-text)`, `#ececea`)을 그대로 사용해 고대비로 보이게 했다.
아이콘은 외부 라이브러리 없이 직접 작성한 인라인 SVG(업로드/지구본/데이터베이스/
설정-슬라이더 4종)이며, CDN 의존성이 없다.

검증: ① `node --check`로 `portal.js` 문법 오류 없음, ② 중괄호 짝 맞춤(45:45)으로
`styles.css` 구문 오류 없음, ③ `npx serve public` 로컬 서버 + `curl`로 3개 파일 모두
HTTP 200 확인, ④ jsdom으로 신규 상호작용 로직을 assert 기반으로 검증(22개 항목 전부
통과) — 그룹 기본 펼침 상태로 자식 3개 라벨 정확히 렌더링, 최상위 2개 feature 라벨
교체 확인, 관리자 링크의 href가 정확하고 `target` 속성이 없음(같은 창 이동)을 확인,
QUERY DATA 클릭 시 ④에 정확한 제목으로 패널 표시, 그룹 헤더 클릭으로 접힘/펼침 토글,
햄버거 클릭 시 `body`에 `sidebar-collapsed` 클래스가 붙고 텍스트형 `sidebar-item`이
전부 사라지며 아이콘 4개만 남음, 접힘 상태에서도 관리자 아이콘 링크가 정확한 href·
target 없음을 유지, 그룹 아이콘 클릭 시 자동으로 펼침 모드+해당 그룹 열림 상태로
복귀 — 전부 통과. **단, 아이콘 모양(업로드/지구본/데이터베이스/설정)이 실제로 각
기능을 직관적으로 대표하는지, 접힘 애니메이션이 매끄러운지는 Claude가 시각적으로
확인하지 못했다** — 실제 배포 페이지에서 직접 클릭해보며 확인 필요.

### 2026-09-22 (9차) — 상단 앱 탭 텍스트 세로 중앙 정렬 수정

사용자가 배포 페이지 스크린샷을 보고 "HSCT가 선택되면 언더바가 나오는 건 좋은
아이디어"라고 6차의 밑줄 방식을 긍정적으로 확인하면서도, "대분류 메뉴명이 너무
올라가 있다"고 지적 — 원인: `.app-tab`이 `height: 100%`로 탭 바 전체 높이를 차지하는데
정작 텍스트 자체는 세로 중앙 정렬이 안 돼 있어서(부모 `.app-tabs`의
`align-items: center`는 버튼 "자신"의 위치만 정렬할 뿐, 버튼 안의 텍스트 위치는
정렬하지 않음), 텍스트가 위쪽에 붙고 밑줄(`border-bottom`)과의 사이가 떠 보였다.
`.app-tab`에 `display: flex; align-items: center;`를 추가해 텍스트 자체를 버튼의
세로 중앙으로 옮기고, 결과적으로 밑줄과 자연스럽게 붙어 보이도록 수정.

검증: ① 중괄호 짝 맞춤(45:45)으로 CSS 구문 오류 없음 확인, ② `npx serve public` 로컬
서버 + `curl`로 `styles.css` HTTP 200 확인, ③ jsdom 22개 assert 전체 재실행 —
전부 통과(레이아웃만 바뀌었고 DOM 구조·클릭 로직은 변경하지 않았으므로 회귀 없음).
**실제 브라우저에서 텍스트가 사용자가 원하는 위치까지 정확히 내려갔는지는 Claude가
시각적으로 확인하지 못했다** — 배포 후 다시 확인 필요.

### 2026-09-22 (10차) — 대분류 탭 선택 시 글자 확대+Bold 적용

사용자 요청: "나중에 레이아웃 2번의 대분류가 늘겠지만, 선택이 되면 메뉴명 글자가 좀
커지면서 BOLD체도 적용되었으면 좋겠어" — 즉 지금은 HSCT 하나뿐이지만 앞으로 대분류
탭이 늘어날 것을 전제로, 선택된 탭이 더 뚜렷하게 도드라지길 원함. `.app-tab.is-active`의
`font-weight`를 `600`(세미볼드) → `700`(진짜 Bold)로, `font-size`를 기본
`0.88rem`에서 `1rem`으로 키움. `.app-tab`에 `font-size` 트랜지션을 추가해 탭 전환 시
부드럽게 커지도록 함. 글자체(폰트 패밀리)는 변경하지 않았다 — 크기·굵기만 조정.

검증: ① 중괄호 짝 맞춤(45:45)으로 CSS 구문 오류 없음 확인, ② `npx serve public` 로컬
서버 + `curl`로 `styles.css` HTTP 200 확인. CSS 전용 변경이라 DOM/클릭 로직에 영향
없음(자바스크립트 파일 변경 없음). **실제 브라우저에서 확대·Bold 효과가 자연스러운
크기감으로 보이는지는 Claude가 시각적으로 확인하지 못했다** — 배포 후 확인 필요.

### 2026-09-22 (11차) — 대분류 탭 텍스트 위치 추가 조정 + 관리자 페이지를 iframe 임베드로 전환 + "준비 중" 배지 제거

세 가지 피드백을 한 번에 반영:

1. **탭 텍스트 위치**: "대분류 글자 좀 내려달라는 게 반영이 안 된 것 같다"는 지적 →
   9차에서 이미 `align-items: center`로 세로 중앙 정렬은 했지만(라이브 CSS로 재확인함,
   실제 반영은 돼 있었음), 순수 중앙 정렬만으로는 사용자가 원하는 만큼(밑줄에 더 가깝게)
   내려가지 않았던 것으로 판단 → `.app-tab`을 `align-items: flex-end` + `padding-bottom:
   10px`로 바꿔, 텍스트가 세로 중앙이 아니라 바닥(밑줄) 쪽에 더 가깝게 앉도록 조정.

2. **관리자 페이지를 실제 ④ 영역 임베드로 전환**: "관리자 페이지는 레이아웃 4번에
   들어와야 하는데 그냥 링크를 걸었네"라는 지적 → 8차에서 `type: 'link'`(브라우저를
   통째로 이동시키는 `<a href>`)로 구현했던 걸, `type: 'embed'`로 바꿔 클릭 시 ④
   영역 안에 `<iframe src="https://hs-code-tool-production.up.railway.app/admin.html">`
   를 띄우도록 변경 — 포털 셸(①②③)을 벗어나지 않고 같은 자리에서 전환되는 README의
   원래 설계 원칙(§1)에 맞춤. **실제로 iframe 삽입이 가능한지 HSCT 서버 응답을 직접
   확인**: `curl -sI https://hs-code-tool-production.up.railway.app/admin.html`로
   `X-Frame-Options`나 `Content-Security-Policy` 헤더가 없음을 확인 — 있었다면
   브라우저가 임베드를 차단했을 것이다. `status: 'ready'`로 표시(유일하게 "연결됨"
   배지가 붙는 실제 동작 항목). 이제 쓰이지 않는 `link` 타입 관련 코드(`buildLink`,
   `.sidebar-link`/`.external-icon` CSS)는 삭제.

3. **"준비 중" 배지 제거**: "준비중이란 글자는 좀 빼보자", 이어서 "레이아웃이 제대로
   되었는지 확인이 어렵고 너무 거슬려"라는 피드백 → 모든 항목에 반복 표시되던 "준비 중"
   배지가 레이아웃 확인을 방해한다고 판단, `statusLabel()`이 `pending` 상태에는 빈
   문자열을 반환하도록 바꾸고, 사이드바 버튼·④ 영역 패널 양쪽에서 배지 텍스트가 없으면
   배지 엘리먼트 자체를 만들지 않도록 수정(빈 배지가 남아 여백만 차지하는 것도 방지).
   `ready` 상태(현재는 관리자 페이지뿐)만 "연결됨" 배지를 유지.

검증: ① `node --check`로 `portal.js` 문법 오류 없음, ② 중괄호 짝 맞춤(43:43)으로
`styles.css` 구문 오류 없음, ③ `npx serve public` 로컬 서버 + `curl`로 3개 파일 모두
HTTP 200 확인, ④ **실제 HSCT 운영 서버에 직접 HTTP 요청을 보내 iframe 차단 헤더가
없음을 확인**(자체 제작 픽스처가 아니라 실제 외부 서비스 응답으로 검증), ⑤ jsdom으로
18개 assert 전체 통과 — 사이드바 어디에도 "준비 중" 텍스트가 없음, 관리자 항목만
"연결됨" 배지 유지·나머지는 배지 엘리먼트 자체가 없음, 관리자 클릭 시 정확한 URL로
iframe이 뜨고 `mainArea`에 `is-embed` 클래스가 붙음, 다른 기능 클릭 시 `is-embed`가
해제되고 기존 패널로 정상 복귀, QUERY DATA 등 그룹 하위 클릭이 리팩터링 후에도 정상
동작, 접힘(아이콘) 모드에서 관리자 아이콘 클릭 시 그룹과 달리 사이드바가 다시 펼쳐지지
않고 접힘 상태를 유지한 채로 iframe만 정상 표시. **단, iframe이 실제 브라우저에서
어색함 없이(스크롤·크기 등) 표시되는지와 탭 텍스트가 원하는 위치까지 내려갔는지는
Claude가 시각적으로 확인하지 못했다** — 배포 후 확인 필요.

### 2026-09-22 (12차) — WGEX 그룹 하위 메뉴 배경을 밝은 톤으로 구분

사용자가 앞서 제시했던 목업 스크린샷에서는 "WGEX 주문내역 업로드"를 펼쳤을 때 하위
3개 항목(DHOLIC 주문내역 업로드/LEGACY FORM/QUERY DATA) 구간이 상위 배경보다 밝은
톤의 박스로 구분돼 있었는데, 8차 구현 때는 왼쪽 얇은 선(`border-left`)만 넣고 배경은
그대로 두어 반영이 누락돼 있었다. `.sidebar-group-children`의 배경을
`var(--color-border)`(`#2b2d31`, 사이드바 배경 `#1d1f22`보다 한 톤 밝음)로 채우고,
좌측 보더를 더 진한 톤(`--color-border-strong`)으로, 모서리를 살짝 둥글려 하나의
패널처럼 보이게 했다. 이 과정에서 하위 항목에 마우스를 올렸을 때 배경(`:hover`)이
새 컨테이너 배경과 같은 색이라 구분이 안 되는 부작용을 발견해, `.sidebar-item.is-nested:hover`
에 더 진한 톤(`--color-border-strong`)을 별도로 지정해 호버 시에도 눈에 띄게 수정.
부수적으로 11차에서 남아있던 주석 문구("준비 중" 패널 표시)도 실제 동작(안내 문구
표시)에 맞게 고쳤다(코드 동작 자체는 변경 없음).

검증: ① `node --check`로 `portal.js` 문법 오류 없음, ② 중괄호 짝 맞춤(44:44)으로
`styles.css` 구문 오류 없음, ③ `npx serve public` 로컬 서버 + `curl`로 `styles.css`·
`portal.js` HTTP 200 확인, ④ jsdom 18개 assert 전체 재실행(DOM 구조·클릭 로직 변경
없음, CSS 전용 수정이므로 회귀 없음 확인). **실제 브라우저에서 밝기 대비가 목업과
비슷한 느낌으로 보이는지는 Claude가 시각적으로 확인하지 못했다** — 배포 후 확인 필요.

### 2026-09-22 (13차) — 홈 화면에 사용 방법 안내 추가 + 대분류 탭 호버 반전 효과

사용자 요청 두 가지 반영:

1. **홈(로고 클릭 시) 화면에 사용법 안내**: 지금까지는 "왼쪽 목록에서 기능을
   선택하세요."라는 한 줄짜리 안내만 있었는데, 포털을 처음 보는 사람도 바로 쓸 수
   있도록 5단계 사용법 목록으로 교체 — ①상단 앱 선택 ②사이드바에서 기능 클릭(WGEX
   그룹은 하위 메뉴가 펼쳐짐을 명시) ③④ 영역에 결과 표시(관리자 페이지만 지금 실제
   동작함을 명시) ④햄버거 아이콘으로 접힘 모드 ⑤로고 클릭 시 언제든 이 화면으로 복귀.
   `portal.js`에 `HOME_STEPS` 배열 + `renderHome()` 함수를 추가하고, 기존
   `renderMain()`의 "activeFeature 없을 때" 분기를 이 함수 호출로 교체했다.
2. **대분류 탭(②) 호버 시 색상 반전**: "레이아웃 2의 대분류도 마우스를 올리면 색상
   반전이 좀 일어나면 좋을 듯" 요청 → `.app-tab:hover`에 배경
   `var(--color-invert-bg)` + 글자색 `var(--color-invert-text)`를 적용. 단, 이전에
   사용자가 "HSCT가 항상 박스 안에 있는 건 촌스럽다"고 지적해 제거했던 것은 **선택된
   탭이 항상 박스로 보이는 것**이었지 호버 시 잠깐 나타나는 반전은 아니므로, 이번
   요청과 상충하지 않는다고 판단해 반영. 구현 과정에서 발견한 문제: `.app-tab`이
   기존에 `height: 100%`로 탭 바 전체 높이를 차지하고 있어서, 그대로 호버 배경을
   넣으면 텍스트 주변이 아니라 탭 바 전체 높이의 세로로 긴 사각형이 하이라이트되는
   어색한 모양이 됨 → `.app-tabs`(부모)를 `align-items: flex-end`로, `.app-tab`
   자체는 `height: 100%`를 없애고 내용에 맞는 자연스러운 높이 + `padding-bottom`으로
   바닥(밑줄) 쪽에 붙게 재구성 — 결과적으로 9차·11차에서 시도했던 "탭 글자 위치
   내리기"도 더 깔끔한 방식으로 다시 해결됨. 추가로, 선택된 탭(밑줄이 진한 글자색)을
   호버하면 반전된 밝은 배경 위에서 밑줄(글자색과 같은 밝은 색)이 안 보이게 되는
   버그를 발견해 `.app-tab.is-active:hover`에서 밑줄 색을 반전된 어두운 색으로
   바꾸도록 별도 처리.

검증: ① `node --check`로 `portal.js` 문법 오류 없음, ② 중괄호 짝 맞춤(49:49)으로
`styles.css` 구문 오류 없음, ③ `npx serve public` 로컬 서버 + `curl`로 3개 파일 모두
HTTP 200 확인, ④ jsdom으로 신규 시나리오까지 포함해 14개 assert 전체 통과 — 초기
로드 시 홈 패널(제목+5단계) 렌더링, 기능 선택 시 홈 패널 사라짐, 로고 클릭 시 홈 패널
복귀, 관리자 페이지 embed 중엔 홈 패널이 없고 iframe만 있음, embed에서 다시 홈으로
돌아오면 `is-embed` 클래스 해제와 함께 홈 패널 복귀, 앱 탭 클릭 시에도 홈으로 리셋.
**단, 탭 호버 시 실제로 밑줄과 반전 배경이 부드럽게 어우러지는지, 홈 화면 안내
목록이 읽기 편한 레이아웃으로 보이는지는 Claude가 시각적으로 확인하지 못했다** —
배포 후 확인 필요.
